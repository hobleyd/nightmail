# Config for `dart run desktop_updater:release ...` (desktop_updater 3.x).
# See https://pub.dev/packages/desktop_updater for the full schema.
#
# The release workflow publishes app-archive.json, each release descriptor and
# release-notes.json under this base URL from the gh-pages branch.
updates:
  baseUrl: https://hobleyd.github.io/nightmail/
